---
import type { Project } from "../data/portfolio";

interface Props {
  project: Project;
}

const { project } = Astro.props;
---

<div class="p-6">
  <div class="flex items-center gap-4">
    <!-- <div class="text-4xl mb-4">{project.icon}</div> -->
    <h3 class="text-xl font-semibold mb-3 text-light-primary">
      {project.title}
    </h3>
  </div>
  <p class="text-light-text-body text-sm mb-4">{project.period}</p>
  <p class="text-light-text-body mb-4">{project.description}</p>

  <div class="flex flex-wrap gap-2 mb-4">
    {
      project.technologies.map((tech) => (
        <span class="px-3 py-1 text-light-accent bg-light-background drop-shadow-lg text-sm">
          {tech}
        </span>
      ))
    }
  </div>

  <div class="text-light-text-body text-sm space-y-1">
    <div class="flex gap-2">
      <p>🎯 <strong>Rol:</strong> {project.role}</p>
      <p>📂 <strong>Categoría:</strong> {project.category}</p>
    </div>
    <div class="mt-2">
      <strong>Características:</strong>
      <ul class="mt-1 space-y-1">
        {
          project.features.map((feature) => (
            <li class="text-xs">• {feature}</li>
          ))
        }
      </ul>
    </div>
  </div>
</div>
