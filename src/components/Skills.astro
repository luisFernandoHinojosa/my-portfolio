---
import SkillsCard from "./SkillsCard.astro";
import { skills } from "../data/portfolio";
---

<section id="habilidades" class="py-20 relative overflow-hidden">
  <div
    class="absolute inset-0 bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900"
  >
  </div>

  <div class="absolute inset-0 overflow-hidden">
    <div
      class="absolute top-1/4 -left-1/4 w-96 h-96 bg-gradient-to-r from-cyan-500/10 to-blue-500/10 rounded-full blur-3xl animate-pulse"
    >
    </div>
    <div
      class="absolute bottom-1/4 -right-1/4 w-96 h-96 bg-gradient-to-r from-purple-500/10 to-pink-500/10 rounded-full blur-3xl animate-pulse"
      style="animation-delay: 2s;"
    >
    </div>
    <div
      class="absolute top-1/2 left-1/2 w-64 h-64 bg-gradient-to-r from-pink-500/5 to-rose-500/5 rounded-full blur-3xl animate-pulse"
      style="animation-delay: 4s;"
    >
    </div>
  </div>

  <!-- Circuit pattern -->
  <div class="absolute inset-0 opacity-10">
    <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
      <defs>
        <pattern
          id="circuit"
          x="0"
          y="0"
          width="20"
          height="20"
          patternUnits="userSpaceOnUse"
        >
          <path
            d="M 0 10 L 10 10 L 10 0 L 20 0 M 10 10 L 10 20 M 10 10 L 20 10"
            stroke="currentColor"
            stroke-width="0.5"
            fill="none"
            opacity="0.3"></path>
        </pattern>
      </defs>
      <rect
        width="100%"
        height="100%"
        fill="url(#circuit)"
        class="text-cyan-400"></rect>
    </svg>
  </div>

  <div class="container mx-auto px-4 relative z-10">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <div class="inline-block">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-12 h-0.5 bg-gradient-to-r from-transparent to-cyan-400">
          </div>
          <span
            class="text-cyan-300 font-mono text-sm uppercase tracking-widest"
          >
            03. Habilidades
          </span>
          <div class="w-12 h-0.5 bg-gradient-to-l from-transparent to-cyan-400">
          </div>
        </div>
      </div>

      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        Stack
        <span
          class="block text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-purple-400 to-pink-400"
        >
          Tecnológico
        </span>
      </h2>

      <p class="text-xl text-slate-300 max-w-2xl mx-auto leading-relaxed">
        Tecnologías y herramientas que domino para crear soluciones digitales
        innovadoras
      </p>
    </div>

    <!-- Skills Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-16">
      {
        skills.map((skill, index) => (
          <div
            class="skill-card transform transition-all duration-500 hover:scale-105"
            style={`animation-delay: ${index * 150}ms`}
          >
            <SkillsCard skill={skill} />
          </div>
        ))
      }
    </div>

    <!-- Skills Summary -->
    <div class="max-w-4xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- Years of experience -->
        <div
          class="text-center p-6 bg-gradient-to-br from-slate-800/50 to-slate-700/30 backdrop-blur-sm rounded-2xl border border-slate-700/50 hover:border-cyan-400/50 transition-all duration-500"
        >
          <div
            class="text-3xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 to-blue-500 mb-2"
          >
            2+
          </div>
          <div class="text-slate-300 font-medium">Años de Experiencia</div>
        </div>

        <!-- Technologies mastered -->
        <div
          class="text-center p-6 bg-gradient-to-br from-slate-800/50 to-slate-700/30 backdrop-blur-sm rounded-2xl border border-slate-700/50 hover:border-purple-400/50 transition-all duration-500"
        >
          <div
            class="text-3xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-500 mb-2"
          >
            {skills.reduce((total, skill) => total + skill.skills.length, 0)}+
          </div>
          <div class="text-slate-300 font-medium">Tecnologías Dominadas</div>
        </div>

        <!-- Projects completed -->
        <div
          class="text-center p-6 bg-gradient-to-br from-slate-800/50 to-slate-700/30 backdrop-blur-sm rounded-2xl border border-slate-700/50 hover:border-pink-400/50 transition-all duration-500"
        >
          <div
            class="text-3xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-pink-400 to-rose-500 mb-2"
          >
            10+
          </div>
          <div class="text-slate-300 font-medium">Proyectos Completados</div>
        </div>
      </div>
    </div>

    <!-- Learning Philosophy -->
    <div class="mt-16 text-center">
      <div
        class="max-w-3xl mx-auto p-8 bg-gradient-to-r from-slate-900/50 to-slate-800/50 backdrop-blur-sm rounded-2xl border border-slate-700/50 hover:border-cyan-400/30 transition-all duration-500"
      >
        <div class="flex items-center justify-center gap-3 mb-4">
          <div
            class="w-8 h-8 bg-gradient-to-r from-cyan-400 to-purple-500 rounded-full flex items-center justify-center"
          >
            <svg
              class="w-4 h-4 text-white"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"
              ></path>
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-white">Aprendizaje Continuo</h3>
        </div>
        <p class="text-slate-300 leading-relaxed">
          Mi filosofía se basa en el aprendizaje constante y la adaptación a las
          nuevas tecnologías. Cada proyecto es una oportunidad para crecer y
          perfeccionar mis habilidades.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .skill-card {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s ease-out forwards;
  }

  @keyframes fadeInUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .skill-card {
      animation: none;
      opacity: 1;
      transform: none;
    }
  }
</style>
